package practice;
											//superclass (Or possible parent class)
public class Bank {							// the base class(Bank), contains one method. (video 26 polymorphism)
	int getInterestRate() {					// this is the parent class, and will be extended to the child classes, the int getInterestRate is intilized to 0, so that 
		return 0;							// the inheritance of the child classes, can be called by a method call
	}

}
